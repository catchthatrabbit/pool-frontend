import React from 'react';
import SearchBar from './SearchBar';

export default {
  title: 'SearchBar',
  component: SearchBar,
};

export const SearchBarBasic = () => (
  <SearchBar
    value="search"
  />
);
